on:
   workflow_dispatch:

jobs:
  use_api:
    runs-on: ubuntu-latest
    permissions: {}
    steps:
      - env:
          GH_TOKEN: ${{ secrets.PACKAGE_PAT }}
        run: |
          gh api /octocat
