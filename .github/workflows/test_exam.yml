name: "Hello Actions"
description: "Greet Someone"
author: "octocat@github.com"

on:
    workflow_dispatch:

inputs:
    MY_NAME:
    description: "Who to greet"
    required: true
    default: "World"

runs:
    usses: "docker"
    image: "Dockerfile"
    
branding:
    icon: "mic"
    color: "purple"
